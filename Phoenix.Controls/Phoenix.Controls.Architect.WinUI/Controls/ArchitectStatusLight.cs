namespace Phoenix.Controls.Architect.WinUI;

/// <summary>
/// Pre-T15 StatusBar traffic-light enum. None = neutral grey dot,
/// Green = success, Yellow = warning, Red = error. Mirrors the
/// pre-T15 StatusBar.TrafficLight values.
/// </summary>
public enum ArchitectStatusLight
{
    None,
    Green,
    Yellow,
    Red,
}
